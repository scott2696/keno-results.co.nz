{
  "_note": "RalfyIndex submission. The API key is NOT here - it lives in the macOS keychain (service 'ralfyindex-api'), because this repo is public and is itself the web root. Endpoint and payload confirmed against the dashboard example: POST /project with apikey, projectName and a urls array.",
  "endpoint": "https://api.ralfyindex.com/project",
  "urlsField": "urls",
  "batchSize": 100,
  "extra": {
    "projectName": "keno-results.co.nz"
  },
  "submit": {
    "article": true,
    "page": true,
    "draw": false,
    "other": false
  },
  "_submit_note": "One credit per URL. Submitted: news and blog articles, plus any page under the pagePrefixes below. Skipped: draw pages (four a day, ~1,460 credits a year, and a dated archive page gains nothing from being indexed in five minutes rather than at the next sitemap crawl) and everything else, which is mostly legal pages that change once a year. Add a prefix to pagePrefixes to include a section.",
  "pagePrefixes": [
    "odds",
    "faqs"
  ]
}
