# Add to .github/workflows/main.yml, immediately AFTER the build step and
# BEFORE the commit step. Needs a repo secret named RALFY_API_KEY
# (Settings -> Secrets and variables -> Actions -> New repository secret),
# because the macOS keychain the local script reads does not exist in CI.

      - name: Submit new URLs for indexing
        env:
          RALFY_API_KEY: ${{ secrets.RALFY_API_KEY }}
        continue-on-error: true
        run: python3 tools/submit_index.py
